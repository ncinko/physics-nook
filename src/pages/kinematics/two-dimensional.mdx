---
layout: ../../layouts/TextbookLayout.astro
title: 2D Kinematics
description: Extend kinematics into two dimensions with vector components, projectile motion, and interactive acceleration sandboxes.
canonicalPath: /kinematics/two-dimensional
---

import SimulationBlock from '../../components/layout/SimulationBlock.astro';
import DerivationPanel from '../../components/textbook/DerivationPanel.astro';
import ProblemSolvingSection from '../../components/textbook/ProblemSolvingSection.astro';
import WorkedExampleCard from '../../components/textbook/WorkedExampleCard.astro';
import ModuleHero from '../../components/modules/ModuleHero.astro';
import HedgehogField2D from '../../components/kinematics/HedgehogField2D';
import LaunchDecomposition from '../../components/kinematics/LaunchDecomposition';
import ProjectileLauncher from '../../components/kinematics/ProjectileLauncher';
import Kinematics2DSandbox from '../../components/kinematics/Kinematics2DSandbox';
import InteractiveAnchor from '../../components/interactive/InteractiveAnchor.astro';
import QuestionSequence from '../../components/interactive/QuestionSequence.jsx';
import { mechanicsPath } from '../../data/modules';

<ModuleHero module={mechanicsPath} currentPath="/kinematics/two-dimensional" />

<div id="page-content"></div>

# 2D Kinematics

In one dimension, position, velocity, and acceleration are each described by one signed number. In two dimensions, each requires two components.  This dimensional scaling is a property of **vector** quantities, unlike **scalars** (e.g. mass and time).

Most of first-year physics relies heavily on the vector formalism, so I suggest building a strong foundation with the concept.  If it's completely new to you, check out the **[Vectors](/math/vectors)** page.

The hedgehog returns, now running a figure eight across a field. We again use vectors to describe its motion: **position** $\vec{r}$ points from the origin to the hedgehog, **velocity** $\vec{v}$ points the way it is running, and **acceleration** $\vec{a}$ points the way its velocity is changing.

<InteractiveAnchor id="hedgehog-field" />

<HedgehogField2D client:load />

The definitions are the same as in one dimension, but it's now clear why we need both a magnitude and direction to describe each quantity.  Written out symbolically, the three kinematic variables are:

$$
\begin{aligned}
\vec{r} &= \langle x, y \rangle,\\
\vec{v} &= \langle v_x, v_y \rangle,\\
\vec{a} &= \langle a_x, a_y \rangle.
\end{aligned}
$$

## Component Motion

For simplicity, let's again consider the case of constant acceleration:

$$
\vec{v}(t) = \vec{v}_i + \vec{a}t,
$$

and

$$
\vec{r}(t) = \vec{r}_i + \vec{v}_it + \frac{1}{2}\vec{a}t^2.
$$

This notation is very concise, but we often like to consider the $x-$ and $y-$ directions independently.  For a baseball flying through the air, these could represent its motion along the horizontal and vertical directions.

$$
\begin{aligned}
v_x(t) &= v_{ix} + a_xt,\\
v_y(t) &= v_{iy} + a_yt,
\end{aligned}
$$

$$
\begin{aligned}
x(t) &= x_i + v_{ix}t + \frac{1}{2}a_xt^2,\\
y(t) &= y_i + v_{iy}t + \frac{1}{2}a_yt^2.
\end{aligned}
$$

This is effectively just a duplicate set of 1D kinematic equations.  Note that these are all functions of a common variable $$t$$. The shared time is what lets us combine the separate one-dimensional calculations into a two-dimensional path. 

The hedgehog's figure eight motion works the same way.  Although its acceleration isn't constant, you can consider $$x(t)$$ and $$y(t)$$ as simple back-and-forth motion in one dimension.


## Launch Velocity

Projectile problems often describe an initial velocity using a launch speed $v_0$ and angle $\theta$.  It's common to use a $$0$$ subscript in place of $$i$$, when $$t=0$$ is implicitly the initial time. The horizontal and vertical pieces are

$$
v_{0x} = v_0\cos\theta,
\qquad
v_{0y} = v_0\sin\theta.
$$

<InteractiveAnchor id="launch-decomposition" />

<LaunchDecomposition client:visible />

## Projectile Motion

A simple projectile has no horizontal acceleration and a constant downward acceleration from gravity:

$$
\vec{a} = \langle 0, -g \rangle,
\qquad
g \approx 9.8\,\text{m/s}^2.
$$

If the projectile starts at the origin, the component equations become

$$
\begin{aligned}
x(t) &= v_{0x}t,\\
y(t) &= v_{0y}t - \frac{1}{2}gt^2,
\end{aligned}
$$

$$
\begin{aligned}
v_x(t) &= v_{0x},\\
v_y(t) &= v_{0y} - gt.
\end{aligned}
$$

The horizontal component of velocity stays constant while the vertical component changes steadily due to gravity. A projectile effectively moves sideways at a steady rate while its height rises, pauses, and then falls.

<DerivationPanel title="Why is the projectile path a parabola?">
  Start with the horizontal equation:

  $$
  x = v_{0x}t.
  $$

  If $v_{0x} \neq 0$, then

  $$
  t = \frac{x}{v_{0x}}.
  $$

  Substitute that time into the vertical equation:

  $$
  y = v_{0y}\left(\frac{x}{v_{0x}}\right)
  - \frac{1}{2}g\left(\frac{x}{v_{0x}}\right)^2.
  $$

  With $v_{0x} = v_0\cos\theta$ and $v_{0y} = v_0\sin\theta$, this can be written as

  $$
  y = (\tan\theta)x -
  \frac{g}{2v_0^2\cos^2\theta}x^2.
  $$

  That is a quadratic function of $x$, so the trajectory is a parabola.
</DerivationPanel>

<InteractiveAnchor id="projectile-launcher" />

<SimulationBlock
  title="Projectile Launcher"
  description="Aim, launch, and compare the predicted range with the actual path. Add drag to see where the ideal equations stop being exact."
>
  <ProjectileLauncher client:only="react" />
</SimulationBlock>

## Acceleration in 2D

For small intervals of time $$\Delta t$$,

$$
\begin{aligned}
\Delta \vec{v} &\approx \vec{a}\Delta t
\end{aligned}
$$

This equation can be interpreted as "acceleration changes the velocity vector".  If the acceleration and velocity vectors are aligned, the velocity gets larger in time: the object speeds up. If acceleration points opposite the velocity, the velocity gets smaller: the object slows down.  What happens when acceleration is perpendicular to velocity?

Look back at the hedgehog at either tip of its figure eight. At these two points, acceleration is perpendicular to velocity. The hedgehog is turning, and for that instant, its speed isn't changing at all. Turn on **decompose a** to see the acceleration vector as: one component parallel to velocity that changes the speed, and another component perpendicular to velocity that turns the hedgehog.

<InteractiveAnchor id="acceleration-sandbox" />

<SimulationBlock
  title="2D Acceleration Sandbox"
  description="Apply a constant acceleration to an object in two dimensions."
>
  <Kinematics2DSandbox client:only="react" />
</SimulationBlock>

<ProblemSolvingSection>
  <WorkedExampleCard
    title="Soccer Ball Components"
    accent="blue"
  >
    #### Problem

    A soccer ball is kicked at $20\,\text{m/s}$ at an angle of $30^\circ$ above the horizontal. Find its initial velocity components.

    #### Given

    - $v_0 = 20\,\text{m/s}$
    - $\theta = 30^\circ$

    #### Use

    $$
    v_{0x} = v_0\cos\theta,
    \qquad
    v_{0y} = v_0\sin\theta.
    $$

    #### Solve

    $$
    v_{0x} = (20)\cos 30^\circ \approx 17.3\,\text{m/s},
    $$

    $$
    v_{0y} = (20)\sin 30^\circ = 10.0\,\text{m/s}.
    $$

    #### Check

    The launch angle is closer to horizontal than vertical, so the horizontal component should be larger than the vertical component. It is.
  </WorkedExampleCard>

  <WorkedExampleCard
    title="Time of Flight and Range"
    accent="blue"
  >
    #### Problem

    Using the same soccer ball, find the time to maximum height, total flight time, and horizontal range. Assume it lands at the same height it was kicked from and ignore air resistance.

    #### Given

    - $v_{0x} \approx 17.3\,\text{m/s}$
    - $v_{0y} = 10.0\,\text{m/s}$
    - $g = 9.8\,\text{m/s}^2$

    #### Solve

    At maximum height, $v_y = 0$:

    $$
    0 = v_{0y} - gt
    \qquad \Rightarrow \qquad
    t = \frac{v_{0y}}{g}
    = \frac{10.0}{9.8}
    \approx 1.02\,\text{s}.
    $$

    For a level landing, the trip down mirrors the trip up, so

    $$
    T \approx 2(1.02) = 2.04\,\text{s}.
    $$

    The horizontal range is

    $$
    R = v_{0x}T \approx (17.3)(2.04)
    \approx 35.3\,\text{m}.
    $$

    #### Check

    A two-second flight at a little over $17\,\text{m/s}$ horizontally should cover a bit more than $34\,\text{m}$, so $35.3\,\text{m}$ is reasonable.
  </WorkedExampleCard>
</ProblemSolvingSection>

<QuestionSequence
  client:load
  eyebrow="2D Kinematics Checkpoint"
  questions={[
    {
      id: 'projectile-horizontal-velocity',
      question: 'In ideal projectile motion with no air resistance, what happens to the horizontal velocity?',
      options: [
        { id: 'B', text: 'It decreases because gravity slows all motion.', isCorrect: false, explanation: 'Gravity changes the vertical component of velocity, not the horizontal component in the ideal projectile model.' },
        { id: 'C', text: 'It increases until the projectile reaches maximum height.', isCorrect: false, explanation: 'Maximum height is determined by $v_y = 0$. It does not require a change in $v_x$.' },
        { id: 'A', text: 'It stays constant because $a_x = 0$.', isCorrect: true, explanation: 'Gravity acts vertically, so the horizontal acceleration is zero. With no horizontal acceleration, $v_x$ remains constant.' }
      ]
    },
    {
      id: 'maximum-height',
      question: 'What is true at the top of an ideal projectile path?',
      options: [
        { id: 'B', text: '$v_y = 0$ but acceleration is still downward.', isCorrect: true, explanation: 'At the top, the vertical velocity momentarily reaches zero. Gravity still acts, so the acceleration remains $-g$ in the vertical direction.' },
        { id: 'A', text: 'Both velocity and acceleration are zero.', isCorrect: false, explanation: 'The vertical velocity is zero at the top, but horizontal velocity can remain nonzero and gravity still provides downward acceleration.' },
        { id: 'C', text: 'The horizontal velocity becomes zero.', isCorrect: false, explanation: 'In ideal projectile motion, the horizontal velocity is unchanged throughout the flight.' }
      ]
    },
    {
      id: 'sideways-acceleration',
      question: 'If acceleration is mostly perpendicular to velocity, what is the immediate effect?',
      options: [
        { id: 'A', text: 'The object must stop.', isCorrect: false, explanation: 'Stopping requires acceleration opposite the velocity for long enough to reduce speed to zero.' },
        { id: 'C', text: 'The path bends because the velocity direction changes.', isCorrect: true, explanation: 'A perpendicular acceleration changes the direction of the velocity vector more than its magnitude, so the path curves.' },
        { id: 'B', text: 'Only the position changes; velocity is unaffected.', isCorrect: false, explanation: 'Acceleration is the rate of change of velocity, so it directly changes the velocity vector.' }
      ]
    }
  ]}
/>
