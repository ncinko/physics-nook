---
layout: ../../layouts/TextbookLayout.astro
title: Electric Current
description: Follow charges into motion — the Drude model of electron drift, Ohm's law, and series–parallel circuits.
canonicalPath: /electromagnetism/current
---

import ModuleHero from '../../components/modules/ModuleHero.astro';
import InteractiveAnchor from '../../components/interactive/InteractiveAnchor.astro';
import QuestionSequence from '../../components/interactive/QuestionSequence.jsx';
import SimulationBlock from '../../components/layout/SimulationBlock.astro';
import DerivationPanel from '../../components/textbook/DerivationPanel.astro';
import ProblemSolvingSection from '../../components/textbook/ProblemSolvingSection.astro';
import WorkedExampleCard from '../../components/textbook/WorkedExampleCard.astro';
import ElectronGas from '../../components/electromagnetism/ElectronGas.jsx';
import ConductorPolarization from '../../components/electromagnetism/ConductorPolarization';
import CircuitSurfaceCharge from '../../components/electromagnetism/CircuitSurfaceCharge';
import CircuitKit from '../../components/electromagnetism/CircuitKit.tsx';
import { electromagnetismPath } from '../../data/modules';

<ModuleHero module={electromagnetismPath} currentPath="/electromagnetism/current" />

<div id="page-content"></div>

# Electric Current

The microscopic motion of charges and the macroscopic behavior of circuits are two views of the same physics. Inside a metal, a sea of conduction electrons collides with the lattice while being nudged along by an electric field. In a circuit, components shape that field to control current and energy transfer. Bridging the two pictures is the essence of **Ohm's law**.

In the classical **Drude model**, a metal hosts a dense gas of conduction electrons that ricochet off the ionic lattice. Each collision resets an electron's random thermal velocity, but an applied field biases the motion into a slow net drift. 

<InteractiveAnchor id="electron-gas" />

<ElectronGas client:only="react" />

The **drift velocity** can be estimated from the average time between collisions $\tau$ and the field $E$:

$$
v_d = -\frac{eE\tau}{m_e},
$$

where $e$ and $m_e$ are the electron charge and mass. The negative sign means electrons drift *opposite* the field. The drift speed is tiny compared with the random thermal speed (about $10^5 \text{m/s}$ at room temperature; the visualization above is *very* exaggerated).  In copper, with $\tau \approx 2.5\times10^{-14}\,\text{s}$ and a field of $1.0\,\text{V/m}$, the drift speed is only about $0.4\,\text{mm/s}$.

<DerivationPanel title="How does this connect to Ohm's law?">

Define the conductivity $\sigma$ and the current density $J = I/A$. Higher conductivity means more current for a given field, and current density is current per cross-sectional area. They are related by

$$
J = \sigma E.
$$

For a wire of length $L$ in a constant field, the potential difference is $\Delta V = EL$, so

$$
J = \sigma E = \sigma\,\frac{\Delta V}{L} \quad\Longrightarrow\quad \Delta V = \frac{L}{\sigma A}\,I.
$$

This is Ohm's law with resistance $R = \dfrac{L}{\sigma A}$.

To connect back to drift, a geometric argument gives the current density as $J = -n e v_d$, where $n$ is the electron number density. Combining with the drift velocity,

$$
v_d = -\frac{eE\tau}{m_e} \quad\Longrightarrow\quad J = \frac{n e^2 \tau}{m_e}\,E,
$$

so the conductivity is

$$
\sigma = \frac{n e^2 \tau}{m_e}.
$$

Materials with more conduction electrons or longer times between collisions conduct better.

</DerivationPanel>

The Drude model is a useful first picture, but it has limits: it treats electrons as classical particles and ignores quantum effects such as the Pauli exclusion principle. Explaining superconductivity or the temperature dependence of resistivity needs more.

## Polarization Dynamics

Before wiring anything up, let's consider the simpler case of a neutral conducting block placed in a uniform electric field.  Its conduction electrons are free to move, so they drift until the net field within the conductor is zero.

As these electrons move, the sides of the block acquire a net positive or net negative charge.  We say a nonzero **surface charge** distribution has formed.  Note that electrons did **not** move from one side to the other; rather, the entire population of conduction electrons simply shifted ever so slightly.  Furthermore, this shift isn't really caused by the electrons pushing each other like a packed crowd of people.  The dynamics are determined by the electric field. 

<InteractiveAnchor id="conductor-polarization" />

<ConductorPolarization client:visible />

<DerivationPanel title="How quickly does a conductor screen a field?">

Charge arriving at a face is just the conduction current that got there, so the surface charge density $\sigma$ grows at the rate

$$
\frac{d\sigma}{dt} = J = \sigma_c E_{\text{in}},
$$

writing $\sigma_c$ for the conductivity to keep it apart from the surface charge. Meanwhile the two charged faces act as a parallel-plate pair, pulling the interior field down from its applied value:

$$
E_{\text{in}} = E_0 - \frac{\sigma}{\varepsilon_0}.
$$

Eliminating $E_{\text{in}}$ leaves a single relaxation equation,

$$
\frac{d\sigma}{dt} = \frac{\sigma_c}{\varepsilon_0}\left(\varepsilon_0 E_0 - \sigma\right)
\quad\Longrightarrow\quad
\sigma(t) = \varepsilon_0 E_0\left(1 - e^{-t/\tau}\right),
\qquad
\tau = \frac{\varepsilon_0}{\sigma_c}.
$$

The interior field decays as $E_0 e^{-t/\tau}$. This **dielectric relaxation time** is startlingly short: copper's $\sigma_c = 5.8\times10^{7}\,\text{S/m}$ gives $\tau = 1.5\times10^{-19}\,\text{s}$.

Note this number is far *below* the collision time $\tau_{\text{coll}} \approx 2.5\times10^{-14}\,\text{s}$ that the Drude model itself depends on, so electrons cannot possibly redistribute that fast. The true settling in a good metal is governed by the collision time and by plasma oscillations of the electron gas, and takes something closer to $10^{-14}\,\text{s}$. In any case, these surface charges form on a very fast time scale.

</DerivationPanel>


## Surface Charge Around a Circuit

A wire carrying a steady current is *not* in electrostatic equilibrium. The electric field inside it is small, but it is not zero.  That field cannot be coming directly from the battery. A battery's own field falls off with distance and has no way to suddenly turn a corner as a wire bends. It instead comes from the surface charge distribution of the circuit.

Start with the switch open.  Every conductor is in static equilibrium, but that does not mean the surfaces are bare.  Each branch is still wired to a battery terminal, so each branch sits at that terminal's potential, and its surface carries some charge. 

Close the switch, and relaxation dynamics similar to the polarized block occur.  However, this time it cannot finish: the battery keeps re-supplying charge to its terminals as electrons begin drifting through the circuit.  Instead of settling into a zero field equilibrium, the surfaces acquire a **gradient** of charge, one that leaves a small electric field pointing along the wire at every point on the loop.

<InteractiveAnchor id="circuit-surface-charge" />

<CircuitSurfaceCharge client:visible />



- Surface charge density tracks the local **potential**.  It is strongly positive near the positive terminal, passing through zero somewhere around the loop, and strongly negative near the negative terminal.
- Its *gradient* sets the interior field, so the charge piles up most steeply where the field has to be strongest. A resistor needs a far larger field than the wire to carry the same current, so a significant change in surface charge happens across it.

## Macroscopic Circuits

The previous section gives a taste of what it means for a circuit to *guide* the electric field.  However, once the circuit has settled, following the charge in that much detail is rarely worth the trouble. It is simpler to track the **potential differences** across components, since a charge carrier's potential energy changes as it moves with or against the field.

A battery provides a potential difference that drives the current. Resistors, bulbs, and other components limit and direct that current, each with its own voltage drop. Applying Ohm's law to each component, together with how the components are wired, predicts the current in every branch.

Build your own circuit below. Drag components from the palette onto the grid, wire them together, and run the simulation. Beyond resistors and batteries, you can add **capacitors**, **inductors**, and **switches**.

<InteractiveAnchor id="circuit-kit" />

<SimulationBlock
  title="Circuit Builder"
  description="Drag components onto the grid, wire them up, and run a transient simulation with a live voltage/current scope. Use Fullscreen for more room."
  width="wide"
>
  <CircuitKit client:only="react" />
</SimulationBlock>

<DerivationPanel title="How does energy flow in a circuit?">

Power is the rate of change of potential energy. For a charge $q$ crossing a potential difference $\Delta V$, the energy change is $\Delta U = q\,\Delta V$. If that charge crosses in time $\Delta t$,

$$
P = \frac{\Delta U}{\Delta t} = \frac{q\,\Delta V}{\Delta t} = I\,\Delta V,
$$

using $I = \dfrac{q}{\Delta t}$. Components with a larger voltage drop or more current dissipate more power. For a resistor, $\Delta V = IR$, so

$$
P = I\,\Delta V = I^2 R,
$$

which is converted into thermal energy, heating the resistor.

</DerivationPanel>

<ProblemSolvingSection title="Problem Solving">
  <WorkedExampleCard title="Drift Speed in Copper" accent="blue">
    #### Problem

    A copper wire $0.10\,\text{m}$ long with cross-section $1.0\,\text{mm}^2$ sits in a field of $2.0\,\text{V/m}$. Estimate the drift speed if $\tau = 2.5\times10^{-14}\,\text{s}$.

    #### Solve

    With $e = 1.60\times10^{-19}\,\text{C}$ and $m_e = 9.11\times10^{-31}\,\text{kg}$,

    $$
    v_d = \frac{eE\tau}{m_e} = \frac{(1.60\times10^{-19})(2.0)(2.5\times10^{-14})}{9.11\times10^{-31}} \approx 8.8\times10^{-3}\,\text{m/s}.
    $$
  </WorkedExampleCard>

  <WorkedExampleCard title="Resistance and Current" accent="blue">
    #### Problem

    Copper has conductivity $5.8\times10^7\,\text{S/m}$. Find the resistance of the wire above, then estimate the current from the field using Ohm's law.

    #### Solve

    $$
    R = \frac{L}{\sigma A} = \frac{0.10}{(5.8\times10^7)(1.0\times10^{-6})} \approx 1.7\times10^{-3}\,\Omega.
    $$

    The potential difference is $\Delta V = EL = (2.0)(0.10) = 0.20\,\text{V}$, so

    $$
    I = \frac{\Delta V}{R} = \frac{0.20}{1.7\times10^{-3}} \approx 120\,\text{A}.
    $$
  </WorkedExampleCard>

  <WorkedExampleCard title="Series vs Parallel" accent="red">
    #### Problem

    Two resistors, $30\,\Omega$ and $60\,\Omega$, are connected across a $12\,\text{V}$ battery. Find the total current if they are (a) in series and (b) in parallel.

    #### Solve

    **(a) Series:** $R_{eq} = 30 + 60 = 90\,\Omega$, so $I = \dfrac{12}{90} \approx 0.13\,\text{A}$.

    **(b) Parallel:** $\dfrac{1}{R_{eq}} = \dfrac{1}{30} + \dfrac{1}{60} = \dfrac{1}{20}$, so $R_{eq} = 20\,\Omega$ and $I = \dfrac{12}{20} = 0.60\,\text{A}$.
  </WorkedExampleCard>
</ProblemSolvingSection>

<QuestionSequence
  client:load
  eyebrow="Electric Current Checkpoint"
  questions={[
    {
      id: 'electron-drift-direction',
      question: 'In the Drude model, which way do conduction electrons drift relative to the applied electric field?',
      options: [
        { id: 'A', text: 'In the same direction as the electric field.', isCorrect: false, explanation: 'Electrons are negatively charged, so their force and drift are opposite the electric field direction.' },
        { id: 'B', text: 'Opposite the electric field direction.', isCorrect: true, explanation: 'Exactly. The drift velocity relation includes the negative electron charge, so electrons drift opposite $\\vec{\\mathbf E}$.' },
        { id: 'C', text: 'Only perpendicular to the field.', isCorrect: false, explanation: 'Collisions randomize the motion, but the small average drift is along or against the field line, not automatically perpendicular.' }
      ]
    },
    {
      id: 'conductor-screening',
      question: 'A copper block has been sitting in a uniform external field for a microsecond. What is the electric field inside the metal?',
      options: [
        { id: 'A', text: 'Zero, because the induced surface charge produces a field that cancels the applied one.', isCorrect: true, explanation: 'Right. Electrons keep drifting until their own faces cancel the applied field, and then there is nothing left to push them.' },
        { id: 'B', text: 'The same as the applied field, since a conductor does not block electric fields.', isCorrect: false, explanation: 'A conductor is exactly what does block a static field: its mobile charge rearranges until the interior field is zero.' },
        { id: 'C', text: 'Larger than the applied field, because the surface charge adds to it.', isCorrect: false, explanation: 'The induced field points against the applied field, not along it. Adding would leave the electrons accelerating forever.' }
      ]
    },
    {
      id: 'surface-charge-gradient',
      question: 'Around a loop of uniform wire carrying a steady current through one resistor, where does the surface charge density change most steeply?',
      options: [
        { id: 'A', text: 'Evenly around the whole loop, since the current is the same everywhere.', isCorrect: false, explanation: 'The current is the same everywhere, but the field needed to drive it is not. The gradient follows the field, not the current.' },
        { id: 'B', text: 'Across the resistor.', isCorrect: true, explanation: 'Exactly. The resistor needs a much stronger internal field to pass the same current, and that field is the gradient of the surface charge.' },
        { id: 'C', text: 'Along the longest stretch of wire, because it accumulates the most charge.', isCorrect: false, explanation: 'A long low-resistance wire drops very little potential, so its surface charge changes only gradually along its length.' }
      ]
    },
    {
      id: 'wire-resistance',
      question: 'For a uniform wire, what change increases its resistance?',
      options: [
        { id: 'A', text: 'Increasing the cross-sectional area while keeping length fixed.', isCorrect: false, explanation: 'A larger area gives charge more conducting path, so it lowers $R=L/(\\sigma A)$.' },
        { id: 'B', text: 'Increasing the conductivity while keeping geometry fixed.', isCorrect: false, explanation: 'Higher conductivity lowers resistance.' },
        { id: 'C', text: 'Increasing the length while keeping area and material fixed.', isCorrect: true, explanation: 'Right. Since $R=L/(\\sigma A)$, a longer wire has more resistance.' }
      ]
    },
    {
      id: 'resistor-power',
      question: 'A resistor carries current $I$ and has voltage drop $\\Delta V$. Which expression gives the power dissipated?',
      options: [
        { id: 'A', text: '$P=I\\Delta V$', isCorrect: true, explanation: 'Correct. Power is energy transfer per time, and charge crossing a voltage drop gives $P=I\\Delta V$.' },
        { id: 'B', text: '$P=I/\\Delta V$', isCorrect: false, explanation: 'Dividing current by voltage does not give power. For a resistor, $P=I\\Delta V=I^2R$.' },
        { id: 'C', text: '$P=R/\\Delta V$', isCorrect: false, explanation: 'That combination has the wrong units for power. The current and voltage drop must be multiplied.' }
      ]
    }
  ]}
/>
