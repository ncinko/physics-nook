---
layout: ../../layouts/TextbookLayout.astro
---

import SimulationBlock from '../../components/layout/SimulationBlock.astro';
import RelativisticMomentumEnergyExplorer from '../../components/relativity/RelativisticMomentumEnergyExplorer.jsx';
import QuestionSequence from '../../components/interactive/QuestionSequence.jsx';
import ModuleHero from '../../components/modules/ModuleHero.astro';
import ProblemSolvingSection from '../../components/textbook/ProblemSolvingSection.astro';
import DerivationPanel from '../../components/textbook/DerivationPanel.astro';
import WorkedExampleCard from '../../components/textbook/WorkedExampleCard.astro';
import { featuredModule } from '../../data/modules';

<ModuleHero module={featuredModule} currentPath="/relativity/momentum-energy" />

<div id="page-content"></div>

# Momentum and Energy

Spacetime diagrams show the geometry of special relativity, and the Lorentz transformations turn that geometry into equations. The next question is dynamical: once an object is moving close to the speed of light, how should we define its momentum and energy?

## Classical Definitions

At everyday speeds, the familiar formulas

$$
p = mv,
\qquad
K = \frac{1}{2}mv^2
$$

work extremely well. Near the speed of light, they do not. These do not build in the fact that no material object can be pushed past $c$, and they underestimate how rapidly the energy cost of speeding up grows as $|v|$ approaches $c$.

Relativity fixes that by changing the momentum and energy formulas while keeping the conservation laws themselves intact.

## Momentum

The cleanest way to write the relativistic formulas is to introduce the dimensionless speed

$$
\beta = \frac{v}{c}
$$

and the Lorentz factor

$$
\gamma = \frac{1}{\sqrt{1-\beta^2}}.
$$

When $|v|$ is small compared with $c$, we have $|\beta| \ll 1$ and $\gamma \approx 1$. As $|v|$ gets closer to $c$, the denominator shrinks and $\gamma$ grows rapidly.

With these, the expression for **relativistic momentum** is

$$
p = \gamma mv 
$$



<DerivationPanel
  title="Why does momentum pick up a gamma factor?"
>
  In classical mechanics, momentum is mass times velocity measured per unit coordinate time:

  $$
  p_{\text{classical}} = m\frac{dx}{dt}.
  $$

  In relativity, different observers do not agree on the same coordinate-time interval $dt$, so it is more natural to build the formula from the particle's **proper time** $d\tau$. Time dilation give the relationship between these two time intervals:

  $$
  d\tau = \frac{dt}{\gamma}
  \qquad \Longrightarrow \qquad
  \frac{dt}{d\tau} = \gamma.
  $$

  A clean relativistic generalization is then

  $$
  p = m\frac{dx}{d\tau}.
  $$

Rewriting that in terms of the ordinary velocity $v = dx/dt$:

  $$
  p = m\frac{dx}{dt}\frac{dt}{d\tau}
  = mv\gamma.
  $$

  This also passes the two checks we want. At low speed, $\gamma \approx 1$, so

  $$
  p \approx mv.
  $$

  And as $|v| \to c$, the factor $\gamma$ grows without bound, so (when applying a force) the momentum keeps increasing instead of allowing a massive particle to reach or exceed the speed of light.
</DerivationPanel>

## Energy

The **total relativistic energy** is

$$
E = \gamma mc^2.
$$

When the particle is at rest, $\gamma = 1$, so the energy does not vanish. Instead it reduces to the **rest energy**

$$
E_0 = mc^2.
$$

The kinetic part is whatever remains after subtracting that rest-energy baseline:

$$
K = E - E_0 = (\gamma - 1)mc^2.
$$


This makes an important distinction clear: rest energy is present even at $v = 0$, while kinetic energy is the extra energy associated with motion.

<DerivationPanel
  title="How do we derive the relativistic energy formula?"
  accent="red"
>
  In one dimension, the incremental work done on a particle is

  $$
  dK = F\,dx.
  $$

  Since $F = dp/dt$ and $v = dx/dt$,

  $$
  dK = \frac{dp}{dt}\,dx = v\,dp.
  $$

  Now substitute the relativistic momentum $p = \gamma mv$:

  $$
  dp = m\,d(\gamma v).
  $$

  Differentiate $\gamma v$ with respect to $v$:

  $$
  \frac{d}{dv}(\gamma v)
  = \gamma + v\frac{d\gamma}{dv}
  = \gamma + \gamma^3\frac{v^2}{c^2}
  = \gamma^3.
  $$

  Therefore

  $$
  dp = m\gamma^3\,dv,
  \qquad
  dK = mv\gamma^3\,dv.
  $$

We can show that 

  $$
  \frac{d\gamma}{dv} = \gamma^3\frac{v}{c^2},
  $$

  so the last expression becomes

  $$
  dK = mc^2\,d\gamma.
  $$

  Integrating from rest ($\gamma = 1$) to speed $v$ gives

  $$
  K = mc^2 \int_1^\gamma d\gamma
  = (\gamma - 1)mc^2.
  $$

  We interpret this kinetic energy as some total energy $\gamma m c^2$ minus a baseline **rest energy** $mc^2$.

</DerivationPanel>

## Energy-momentum Relation

The momentum and energy formulas are tied together. Starting from

$$
E = \gamma mc^2,
\qquad
p = \gamma mv,
$$

we can eliminate $\gamma$ by computing

$$
E^2 - p^2c^2
= \gamma^2 m^2 c^4 - \gamma^2 m^2 v^2 c^2.
$$

Factor out $\gamma^2 m^2 c^4$:

$$
E^2 - p^2c^2
= \gamma^2 m^2 c^4 \left(1 - \frac{v^2}{c^2}\right)
= \gamma^2 m^2 c^4 (1 - \beta^2).
$$

Since $\gamma^2 = 1/(1-\beta^2)$, the final factor cancels:

$$
E^2 - p^2c^2 = m^2 c^4.
$$

This invariant relation is often written as

$$
E^2 = p^2c^2 + m^2 c^4.
$$

Not only is this true for massive particles, but it happens to work for massless particles as well.  For example, the energy of a photon can be written as $E = pc$.  This immediately begs the question: **how do we define the momentum carried by a photon?**  Classical electromagnetic waves carry energy and momentum, but the answer ultimately requires quantum mechanics if you want a massless particle theory of light.






<ProblemSolvingSection>
  <WorkedExampleCard
    title="Compute Momentum and Energy from Beta"
    accent="blue"
  >
    #### Problem

    A particle moves with $\beta = 0.60$. Find $\gamma$, $p/(mc)$, $E/(mc^2)$, and $K/(mc^2)$.

    #### Given

    - $\beta = 0.60$

    #### Use

    $$
    \gamma = \frac{1}{\sqrt{1-\beta^2}},
    \qquad
    \frac{p}{mc} = \gamma\beta,
    \qquad
    \frac{E}{mc^2} = \gamma,
    \qquad
    \frac{K}{mc^2} = \gamma - 1.
    $$

    #### Solve

    $$
    \gamma = \frac{1}{\sqrt{1-0.60^2}}
    = \frac{1}{\sqrt{0.64}}
    = 1.25.
    $$

    Then

    $$
    \frac{p}{mc} = (1.25)(0.60) = 0.75,
    $$

    $$
    \frac{E}{mc^2} = 1.25,
    \qquad
    \frac{K}{mc^2} = 1.25 - 1 = 0.25.
    $$

    #### Check

    The total energy stays above $1$ because the rest-energy piece is always present, while the kinetic part is only the extra amount above that baseline.
  </WorkedExampleCard>

  <WorkedExampleCard
    title="Recover Speed from Total Energy"
    accent="red"
  >
    #### Problem

    A particle has total energy $E = 2mc^2$. Find its Lorentz factor $\gamma$, its speed parameter $\beta$, and its kinetic energy $K$.

    #### Given

    - $E = 2mc^2$

    #### Use

    Since

    $$
    E = \gamma mc^2,
    $$

    we have $\gamma = E/(mc^2)$. Then

    $$
    \beta = \sqrt{1 - \frac{1}{\gamma^2}},
    \qquad
    K = (\gamma - 1)mc^2.
    $$

    #### Solve

    $$
    \gamma = 2.
    $$

    So

    $$
    \beta = \sqrt{1 - \frac{1}{2^2}}
    = \sqrt{\frac{3}{4}}
    \approx 0.866.
    $$

    The kinetic energy is

    $$
    K = (2 - 1)mc^2 = mc^2.
    $$

    #### Check

    Half of the total energy is the rest-energy baseline and the other half is kinetic energy. A speed of about $0.87c$ is consistent with a particle that is energetic but still below the speed of light.
  </WorkedExampleCard>
</ProblemSolvingSection>

<QuestionSequence
  client:load
  eyebrow="Relativistic Dynamics Checkpoint"
  questions={[
    {
      id: 'momentum-sign',
      question: 'A particle changes from $\\beta = +0.60$ to $\\beta = -0.60$. Which statement is correct?',
      options: [
        { id: 'B', text: 'Its momentum and total energy both change sign because the velocity changed sign.', isCorrect: false, explanation: 'Momentum changes sign in one dimension, but total energy does not. The relativistic energy depends on $\\gamma$, and $\\gamma$ stays positive for both $+0.60$ and $-0.60$.' },
        { id: 'C', text: 'Its momentum stays the same because $\\gamma$ stays the same.', isCorrect: false, explanation: 'Keeping the same $\\gamma$ does not keep the momentum fixed, because $p = \\gamma m v$ still contains the signed velocity. The direction flip changes the sign of $p$.' },
        { id: 'A', text: 'Its momentum changes sign, but its total energy and kinetic energy stay the same.', isCorrect: true, explanation: 'Exactly. The sign of $p$ comes from the sign of $\\beta$, so reversing direction flips the momentum. But $\\gamma$ depends on $\\beta^2$, so both $E = \\gamma mc^2$ and $K = (\\gamma - 1)mc^2$ stay unchanged when only the sign of the velocity is reversed.' }
      ]
    },
    {
      id: 'rest-vs-kinetic',
      question: 'What does the formula $K = (\\gamma - 1)mc^2$ imply when the particle is at rest?',
      options: [
        { id: 'A', text: 'At rest, $K = 0$ while the total energy is still $E_0 = mc^2$.', isCorrect: true, explanation: 'Right. When $v = 0$, we have $\\beta = 0$ and $\\gamma = 1$. That makes the kinetic term vanish, but the particle still carries rest energy $mc^2$.' },
        { id: 'B', text: 'At rest, both kinetic energy and total energy are zero.', isCorrect: false, explanation: 'Only the kinetic part is zero at rest. Relativity keeps the rest-energy term $mc^2$, so the total energy does not vanish.' },
        { id: 'C', text: 'At rest, the kinetic energy equals the rest energy because both come from the mass.', isCorrect: false, explanation: 'Rest energy and kinetic energy are different pieces of the bookkeeping. At rest there is no motion contribution, so the kinetic part is zero even though the rest energy remains.' }
      ]
    },
    {
      id: 'classical-limit',
      question: 'Why do the classical formulas work well when $|\\beta| \\ll 1$?',
      options: [
        { id: 'B', text: 'Because relativity only applies above a fixed speed threshold and turns off below it.', isCorrect: false, explanation: 'Relativity does not switch on or off at a threshold. The same relativistic formulas always apply, but for small $|\\beta|$ they are well approximated by the classical expressions.' },
        { id: 'A', text: 'Because then $\\gamma \\approx 1 + \\tfrac{1}{2}\\beta^2$, which gives $p \\approx mv$ and $K \\approx \\tfrac{1}{2}mv^2$.', isCorrect: true, explanation: 'Exactly. At low speed, the Lorentz factor is only slightly larger than $1$, so the relativistic formulas reduce to the familiar classical ones as an approximation.' },
        { id: 'C', text: 'Because momentum and energy stop being conserved near the speed of light.', isCorrect: false, explanation: 'Conservation laws still hold at all speeds. What changes near $c$ is the correct formula for the momentum and energy of each particle.' }
      ]
    }
  ]}
/>
