---
layout: ../../layouts/TextbookLayout.astro
title: Measurement & Uncertainty
description: Discover π by measuring round objects — read the ± off a ruler, propagate it through C ÷ d, and test whether your error bar catches the true value.
canonicalPath: /measurement/uncertainty
---

import ModuleHero from '../../components/modules/ModuleHero.astro';
import InteractiveAnchor from '../../components/interactive/InteractiveAnchor.astro';
import QuestionSequence from '../../components/interactive/QuestionSequence.jsx';
import ProblemSolvingSection from '../../components/textbook/ProblemSolvingSection.astro';
import WorkedExampleCard from '../../components/textbook/WorkedExampleCard.astro';
import { ChickenCompanion } from '../../components/measurement/ChickenCompanion';
import { UncertainRuler } from '../../components/measurement/UncertainRuler';
import { PropagationBracket } from '../../components/measurement/PropagationBracket';
import { PiHunt } from '../../components/measurement/PiHunt';
import { measurementPath } from '../../data/modules';

<ChickenCompanion action="openGame" client:only="react" />

<ModuleHero module={measurementPath} currentPath="/measurement/uncertainty" />

<div id="page-content"></div>

# Uncertainty

Dividing the circumference of a circle by its diameter always gives the same value of $\pi = 3.14159\ldots$  In theory, we should be able to *measure* $\pi$ ourselves. Wrap a measuring tape around a mug, place a ruler across it, and divide our two results.

Trouble shows up the moment you read the tape or ruler: the edges never lands exactly on a mark.  A measurement is always a **best estimate** together with an **uncertainty**, written as

$$
\text{measurement} = (\text{best estimate} \pm \text{uncertainty}) \times \text{unit}.
$$

When you read a single mark off a ruler, the edge falls somewhere inside the smallest division. Half of that smallest division is a standard choice of uncertainty.

<InteractiveAnchor id="uncertain-ruler" />

<UncertainRuler client:visible />

Something like $\pm 0.5\ \text{mm}$ expresses the **absolute** uncertainty, which carries units. Dividing it by the measurement gives the **relative** (percent) uncertainty: $0.5\ \text{mm}$ on a $200\ \text{mm}$ length is only $0.25\%$, but on a $5\ \text{mm}$ length it is a hefty $10\%$. It can be difficult to measure small things.

Furthermore, the number of digits used implies something about the uncertainty as well. Reporting $24.8\ \text{cm}$ implies the uncertainty is on the order of $0.1 \ \text{cm}$. We often round the uncertainty to **one significant figure**, then round the measured value to the same decimal place: $3.097 \pm 0.083$ is reported as $3.10 \pm 0.08$.

## Propagation of uncertainty

To get $\pi$ we have to *divide* one uncertain number by another. So how do we quantify the uncertainty of the resulting quotient? 

The simplest method is to just try the extremes.  Take the smallest and largest plausible value of each input, recompute the result every which way, and see how far apart the answers land. The range of values quantifies the uncertainty; the half-width of this range is the new $\pm$. 

<InteractiveAnchor id="propagation-bracket" />

<PropagationBracket client:visible />

Once you've done this procedure a few times, you may start to notice a pattern:

**Addition/Subtraction: absolute uncertainties add.**  If $R = A \pm B$, then $\delta R = \delta A + \delta B$.

**Multiplication/Division: relative uncertainties (approximately) add.**  If $R = A \times B$ or $R = A / B$, then

$$
\frac{\delta R}{|R|} = \frac{\delta A}{|A|} + \frac{\delta B}{|B|}.
$$

Note that when you subtract two measurements, the absolute uncertainties still add.  If the two measurements are nearly equal, the result itself is tiny — so the *relative* uncertainty can blow up. Measuring a thin gap as the difference of two lengths is a good way to get a useless answer.

## Comparing your result to theory

If you've followed these procedures, each measurement isn't a single value.  Instead, it's a *range* around your best estimate of the value: from $\text{estimate} - \text{uncertainty}$ to $\text{estimate} + \text{uncertainty}$. Drawn on a graph, that range is an **error bar**.

This is a good time to clarify the scientific meaning of two similar words: **error** and **uncertainty**.  Error is the *difference between a measured value and the true value*.  Since the true value of something is rarely known, we don't know the error in our measurements (if we did, we'd just take better measurements).  The best we can do is *estimate the error*, i.e. express uncertainty.

Rather than compare results to some true value, we compare them to a theoretical prediction.  In this case, theory predicts that $\pi \approx 3.14$ (nevermind that this is a rare instance in which we know the *true value*).  **Theory agrees with your measurement when it falls within your uncertainty range.** 

<InteractiveAnchor id="pi-hunt" />

<PiHunt client:visible />

By random chance, it's common to have some measurements in disagreement with the theoretical value.  If the magnitude of this disagreement is improbable (*statistics* can help quantify this probability), either you made a mistake or there's a **systematic error** — a consistent bias, like a tape that was read off-center every time — that your uncertainty didn't account for. 

It's helpful to quantify any discrepancy as a multiple of the uncertainty:

$$
\frac{|\,\text{experiment} - \text{theory}\,|}{\text{uncertainty}}.
$$

If this quantity is about $1$, the uncertainty is likely a good estimate of the error; very close to $0$ or much greater than $2$ typically means something is wrong. This is also where **precision** and **accuracy** differ: small values of the numerator correspond to accurate measurements, while small values of the denominator are precise.  As an experimentalist, the goal is usually to make both *as small as possible* while keeping a ratio close to 1.

<ProblemSolvingSection
  title="Problem Solving"
>
  <WorkedExampleCard
    title="Area of a tabletop"
    accent="blue"
  >
    #### Problem

    A tabletop measures $L = 120.0 \pm 0.5\ \text{cm}$ by $W = 80.0 \pm 0.5\ \text{cm}$. Find its area and the uncertainty in that area.

    #### Use

    Area is a product, so relative uncertainties add:

    $$
    \frac{\delta A}{A} = \frac{\delta L}{L} + \frac{\delta W}{W}.
    $$

    #### Solve

    $$
    A = 120.0 \times 80.0 = 9600\ \text{cm}^2,
    $$

    $$
    \frac{\delta A}{A} = \frac{0.5}{120.0} + \frac{0.5}{80.0} = 0.42\% + 0.63\% = 1.04\%.
    $$

    $$
    \delta A = 9600 \times 0.0104 \approx 100\ \text{cm}^2 \;\Rightarrow\; A = 9600 \pm 100\ \text{cm}^2.
    $$

    #### Check

    The bigger side carries the smaller relative uncertainty, and the percentages simply add — exactly what the high–low machine shows for a product.
  </WorkedExampleCard>

  <WorkedExampleCard
    title="A thin gap"
    accent="red"
  >
    #### Problem

    Two scratches sit at $x_2 = 12.4 \pm 0.1\ \text{cm}$ and $x_1 = 11.9 \pm 0.1\ \text{cm}$. How wide is the gap between them?

    #### Use

    The gap is a difference, so absolute uncertainties add:

    $$
    \delta(\Delta x) = \delta x_2 + \delta x_1.
    $$

    #### Solve

    $$
    \Delta x = 12.4 - 11.9 = 0.5\ \text{cm}, \qquad \delta(\Delta x) = 0.1 + 0.1 = 0.2\ \text{cm}.
    $$

    So $\Delta x = 0.5 \pm 0.2\ \text{cm}$ — a relative uncertainty of $40\%$.

    #### Check

    Each length was known to under $1\%$, yet their difference is uncertain to $40\%$. Subtracting nearly equal numbers throws precision away; measure the gap directly if you can.
  </WorkedExampleCard>

  <WorkedExampleCard
    title="Does this circle give π?"
    accent="blue"
  >
    #### Problem

    A plate gives a circumference $C = 69.5 \pm 0.2\ \text{cm}$ and a diameter $d = 22.0 \pm 0.1\ \text{cm}$. Find $C/d$ with its uncertainty, and decide whether it agrees with $\pi = 3.14159$.

    #### Solve

    $$
    \frac{C}{d} = \frac{69.5}{22.0} = 3.159, \qquad
    \frac{\delta(C/d)}{C/d} = \frac{0.2}{69.5} + \frac{0.1}{22.0} = 0.29\% + 0.45\% = 0.74\%.
    $$

    $$
    \delta(C/d) = 3.159 \times 0.0074 \approx 0.02 \;\Rightarrow\; \frac{C}{d} = 3.16 \pm 0.02.
    $$

    #### Verdict

    The range is $[3.14,\ 3.18]$, and $\pi = 3.14159$ lands just inside it — **consistent**, though only barely. The discrepancy is about $\tfrac{|3.16 - 3.142|}{0.02} \approx 0.9$ of an uncertainty. A finer tape would tighten the bar and make the test sharper.
  </WorkedExampleCard>
</ProblemSolvingSection>

<QuestionSequence
  client:load
  eyebrow="Uncertainty Checkpoint"
  questions={[
    {
      id: 'which-rule',
      question: 'You compute a speed $v = d/t$ from a measured distance and time. How do you get the uncertainty in $v$?',
      options: [
        { id: 'A', text: 'Add the relative (percent) uncertainties of $d$ and $t$.', isCorrect: true, explanation: 'Division combines quantities multiplicatively, so relative uncertainties add: $\\delta v / v = \\delta d / d + \\delta t / t$.' },
        { id: 'B', text: 'Add the absolute uncertainties $\\delta d$ and $\\delta t$.', isCorrect: false, explanation: 'Absolute uncertainties add only for sums and differences. They do not even share units with $v$ here.' },
        { id: 'C', text: 'Subtract the relative uncertainty of $t$ from that of $d$.', isCorrect: false, explanation: 'Uncertainties never cancel — combining measurements can only increase the uncertainty, so the relative uncertainties add.' }
      ]
    },
    {
      id: 'compare-g',
      question: 'A lab measures $g = 9.6 \\pm 0.3\\ \\text{m/s}^2$. The accepted value is $9.81\\ \\text{m/s}^2$. Is the result consistent with theory?',
      options: [
        { id: 'A', text: 'Yes — $9.81$ lies inside the range $[9.3,\\ 9.9]$.', isCorrect: true, explanation: 'The error bar runs from $9.6 - 0.3$ to $9.6 + 0.3$, and $9.81$ falls inside it, so the measurement is consistent.' },
        { id: 'B', text: 'No — the best estimate $9.6$ is not equal to $9.81$.', isCorrect: false, explanation: 'A measurement is a range, not a single number. Agreement means the accepted value sits inside the error bar, not that the best estimates match exactly.' },
        { id: 'C', text: "Can't tell without more measurements.", isCorrect: false, explanation: 'The reported uncertainty is enough to run the error-bar test right now.' }
      ]
    },
    {
      id: 'precision-accuracy',
      question: 'Five repeated readings cluster tightly together but sit well above the true value. How would you describe them?',
      options: [
        { id: 'A', text: 'Precise but not accurate — a likely systematic error.', isCorrect: true, explanation: 'Tight scatter means high precision; a consistent offset from the truth means low accuracy, the signature of a systematic bias.' },
        { id: 'B', text: 'Accurate but not precise.', isCorrect: false, explanation: 'Accurate means centered on the truth; these are offset. Tight clustering is precision, not accuracy.' },
        { id: 'C', text: 'Both accurate and precise.', isCorrect: false, explanation: 'They are precise, but being offset from the true value means they are not accurate.' }
      ]
    },
    {
      id: 'reporting',
      question: 'A calculation gives $3.14159$ with a raw uncertainty of $0.023$. How should you report it?',
      options: [
        { id: 'A', text: '$3.14 \\pm 0.02$', isCorrect: true, explanation: 'Round the uncertainty to one significant figure ($0.02$), then round the value to the same decimal place ($3.14$).' },
        { id: 'B', text: '$3.14159 \\pm 0.023$', isCorrect: false, explanation: 'The value claims far more precision than the uncertainty allows. Match the value to the rounded uncertainty.' },
        { id: 'C', text: '$3.1 \\pm 0.02$', isCorrect: false, explanation: 'The uncertainty is in the hundredths place, so the value should be kept to the hundredths place too: $3.14$.' }
      ]
    }
  ]}
/>
