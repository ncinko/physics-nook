---
layout: ../../layouts/TextbookLayout.astro
title: Electric Potential
description: Move from force to energy — potential energy landscapes, the electric potential, and its relationship to the field.
canonicalPath: /electromagnetism/potential
---

import ModuleHero from '../../components/modules/ModuleHero.astro';
import InteractiveAnchor from '../../components/interactive/InteractiveAnchor.astro';
import QuestionSequence from '../../components/interactive/QuestionSequence.jsx';
import SimulationBlock from '../../components/layout/SimulationBlock.astro';
import DerivationPanel from '../../components/textbook/DerivationPanel.astro';
import ProblemSolvingSection from '../../components/textbook/ProblemSolvingSection.astro';
import WorkedExampleCard from '../../components/textbook/WorkedExampleCard.astro';
import PotentialEnergyLandscape from '../../components/electromagnetism/PotentialEnergyLandscape.jsx';
import ScalarPotential2D from '../../components/electromagnetism/ScalarPotential2D.jsx';
import TopographicLandscape from '../../components/electromagnetism/TopographicLandscape';
import ElectricPotentialExplorer from '../../components/electromagnetism/ElectricPotentialExplorer.jsx';
import { electromagnetismPath } from '../../data/modules';

<ModuleHero module={electromagnetismPath} currentPath="/electromagnetism/potential" />

<div id="page-content"></div>

# Electric Potential

The **electric potential** describes electric interactions in terms of *energy* rather than *force*. Just as gravitational potential energy relates to the gravitational force, electric potential energy relates to the electric force. When a charge moves through a field, its potential energy changes; we define the electric potential $V$ as the potential energy per unit charge.



## Potential Energy Landscapes

Visualizing an object "rolling" on a potential energy surface connects the force and energy pictures. Below, a mass feels either a spring-like force or simple gravity. In both cases it tends to move *downhill* along the 1D potential energy curve, trading potential energy for kinetic energy and back.

<InteractiveAnchor id="potential-energy-landscape" />

<PotentialEnergyLandscape client:only="react" />

This downhill tendency is a general property of conservative forces. The force always points toward decreasing potential energy, captured in one dimension by

$$
F \approx -\frac{\Delta U}{\Delta x},
$$

which is the slope of the potential energy function $U(x)$.

<DerivationPanel title="Which forces have a potential energy?">

The work done by a **conservative** force depends only on the start and end positions, not the path taken. Those are exactly the forces for which we can define a scalar potential energy. The electric, gravitational, and spring forces are conservative; friction and air resistance are not.

In one dimension, the work done over a small step is $W \approx F\,\Delta x$. For a conservative force we call this a change in potential energy, $\Delta U = -W$, so

$$
F \approx -\frac{\Delta U}{\Delta x}.
$$

In three dimensions the same idea is the gradient, which points in the direction of steepest increase of a scalar field:

$$
\vec{\mathbf F} = -\nabla U = -\left\langle \frac{\partial U}{\partial x}, \frac{\partial U}{\partial y}, \frac{\partial U}{\partial z} \right\rangle.
$$

Don't worry if that notation is unfamiliar — picture a 2D energy landscape $U(x,y)$ of hills and valleys, with matter tending to flow downhill.

</DerivationPanel>

## From Energy to Potential

Before the electrical case, recall gravitational potential energy near Earth's surface, $U = mgh$.

<DerivationPanel title="Does U = mgh agree with our definition?">

Yes. With "up" positive, the gravitational force near the surface is $F = -mg$. The work done moving an object up a small height $\Delta h$ is $W = F\,\Delta h = -mg\,\Delta h$, so

$$
\Delta U = -W = mg\,\Delta h.
$$

Choosing $U = 0$ at $h = 0$ gives $U(h) = mgh$.

</DerivationPanel>

Suppose we want the potential energy *independent of mass*. Define $V = gh$ with the rule $U = mV$. This $V$ (units of J/kg) tells us the potential energy per unit mass at a given height.

For the electric force we do exactly this. The electric potential $V$ is defined so that

$$
U = qV,
$$

where $q$ is the charge. Its units are J/C, which we call **volts**. Only *changes* in potential energy — and therefore changes in potential — are physical: $V$ can be shifted by any constant without changing the physics, just as elevation does not depend on our choice of sea level. Picking that constant is choosing a "zero of potential."

## Reading a Landscape with Contour Lines

Imagine walking around a hill without climbing or descending. Your path follows a **contour line**: every point on it has the same elevation. A topographical map draws these lines from above, marking heights at a fixed interval.


<InteractiveAnchor id="topographic-landscape" />

<TopographicLandscape client:visible />

The lines are equally spaced in **height**, not in horizontal distance. Crossing several lines over a short distance means a steep slope. Electric **equipotential lines** work the same way, with volts replacing meters: each line joins points of equal potential, and closer lines at a fixed voltage interval indicate a stronger electric field. 

## Point Charge Potential

The electric potential due to a point charge $Q$ is

$$
V = k\,\frac{Q}{r},
$$

where $r$ is the distance from the charge and $k = 9 \times 10^9\ \tfrac{\text{N·m}^2}{\text{C}^2}$.

<DerivationPanel title="Where does V = kQ/r come from?">

Start from $V = \tfrac{U}{q}$, so we need the potential energy of a charge $q$ in the field of $Q$. Coulomb's law gives the force $F = k\dfrac{Qq}{r^2}$. The change in potential energy bringing $q$ from infinity to $r$ is

$$
\Delta U = -\int_{\infty}^{r} k\,\frac{Qq}{r^2}\,dr = -kQq\left[-\frac{1}{r}\right]_{\infty}^{r} = k\,\frac{Qq}{r}.
$$

Dividing by $q$,

$$
V = \frac{U}{q} = k\,\frac{Q}{r}.
$$

</DerivationPanel>
This choice of potential function implicitly chooses the **zero potential** to be infinitely far away; $V(r \to \infty) \to 0$.  The important feature of the function is the $1/r$ proportionality.  There are many other functions that would work perfectly well.  

Although not as common, it can be fun to visualize this electric potential function in 2D with a color map. 

<InteractiveAnchor id="scalar-potential-2d" />

<ScalarPotential2D client:only="react" />

In 3D we draw *equipotential surfaces*, the analog of the equipotential lines shown above. Because the **electric potential energy** $U = qV$, positive and negative charges behave differently in the *same* potential: both tend toward lower *potential energy*, not lower *potential*. When the signs get confusing, fall back on "like charges repel, opposites attract."

This last explorer overlays the potential color map and equipotentials in the space around a collection of point charges.  Note that **zero potential** isn't always infinitely far away.

<InteractiveAnchor id="electric-potential-explorer" />

<SimulationBlock
  title="Electric Potential Explorer"
  description="Toggle the colormap, equipotentials, field arrows, and field lines; add, drag, and remove charges."
>
  <ElectricPotentialExplorer client:only="react" />
</SimulationBlock>

The explorer uses one voltage interval throughout each view, displayed below the plot. It adapts that interval to the charges and leaves out extreme values near the charge cores to keep the drawing readable.

Notice that the equipotential lines are everywhere perpendicular to the field lines. Just as $F \approx -\tfrac{\Delta U}{\Delta x}$ in one dimension, the field is the (negative) slope of the potential:

$$
E \approx -\frac{\Delta V}{\Delta x}.
$$

<ProblemSolvingSection title="Problem Solving">
  <WorkedExampleCard title="Potential of a Point Charge" accent="blue">
    #### Problem

    What is the electric potential $0.25\,\text{m}$ from a $+3\,\mu\text{C}$ point charge?

    #### Solve

    $$
    V = k\,\frac{q}{r} = 9\times10^9 \cdot \frac{3\times10^{-6}}{0.25} \approx 1.1\times10^5\,\text{V}.
    $$
  </WorkedExampleCard>

  <WorkedExampleCard title="Work Along an Equipotential" accent="blue">
    #### Problem

    Why is no work required to move a charge along an equipotential surface?

    #### Reasoning

    Along an equipotential, $\Delta V = 0$. Since $\Delta U = q\,\Delta V$, the change in potential energy is zero — and the work done by the electric force equals $-\Delta U = 0$.
  </WorkedExampleCard>

  <WorkedExampleCard title="Change in Potential Energy" accent="red">
    #### Problem

    Find the change in electric potential energy of a $-3\,\mu\text{C}$ charge that moves from $5\,\text{V}$ to $12\,\text{V}$.

    #### Solve

    With $\Delta V = 7\,\text{V}$ and $q = -3\times10^{-6}\,\text{C}$,

    $$
    \Delta U = q\,\Delta V = (-3\times10^{-6})(7) = -2.1\times10^{-5}\,\text{J}.
    $$

    The negative charge *loses* potential energy moving toward higher potential.
  </WorkedExampleCard>
</ProblemSolvingSection>

<QuestionSequence
  client:load
  eyebrow="Electric Potential Checkpoint"
  questions={[
    {
      id: 'potential-definition',
      question: 'What does electric potential measure?',
      options: [
        { id: 'A', text: 'The force per unit charge at a point.', isCorrect: false, explanation: 'Force per unit charge is electric field. Electric potential is tied to energy per unit charge.' },
        { id: 'B', text: 'The total force on every charge in the region.', isCorrect: false, explanation: 'Potential is a scalar field, not a force ledger. Force depends on the slope of the potential and on the charge.' },
        { id: 'C', text: 'Electric potential energy per unit charge.', isCorrect: true, explanation: 'Exactly. The defining relation is $V=U/q$, or equivalently $U=qV$.' }
      ]
    },
    {
      id: 'equipotential-work',
      question: 'Why does the electric force do no net work when a charge moves along an equipotential?',
      options: [
        { id: 'A', text: 'Because $\\Delta V=0$, so $\\Delta U=q\\Delta V=0$.', isCorrect: true, explanation: 'Right. No change in potential means no change in electric potential energy.' },
        { id: 'B', text: 'Because the electric field must be zero everywhere on an equipotential.', isCorrect: false, explanation: 'The field does not have to be zero. It is perpendicular to the equipotential surface in the ideal electrostatic picture.' },
        { id: 'C', text: 'Because only positive charges can move along equipotentials.', isCorrect: false, explanation: 'Either sign of charge can move along an equipotential. The key is that the potential does not change along the path.' }
      ]
    },
    {
      id: 'field-from-potential',
      question: 'In one dimension, how is the electric field related to the electric potential?',
      options: [
        { id: 'A', text: 'The field points toward increasing potential for every charge.', isCorrect: false, explanation: 'The electric field points in the direction of decreasing potential, while the force on a charge also depends on the charge sign.' },
        { id: 'B', text: 'The field is the negative slope of the potential, $E\\approx-\\Delta V/\\Delta x$.', isCorrect: true, explanation: 'Correct. This is the electric version of a force pointing downhill on an energy landscape.' },
        { id: 'C', text: 'The field is unrelated to the potential once charges start moving.', isCorrect: false, explanation: 'The field-potential relationship is exactly what lets the scalar potential describe electric forces in electrostatics.' }
      ]
    }
  ]}
/>
